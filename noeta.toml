[package]
name = "para/aether"
version = "0.1.1"
# The minimum noeta this package works with; enforced at resolve time from noeta 0.2.1 on.
toolchain = ">=0.2"
description = "The Noeta web framework: routing, middleware, dependency injection, sessions, and TLS."
keywords = ["web", "framework", "http", "server", "routing"]
